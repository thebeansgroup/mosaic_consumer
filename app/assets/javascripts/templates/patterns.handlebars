<div class="navbar">
  <ul class="navbar_list">
    {{#each tag in allTags}}
      <li>{{#linkTo 'tag' tag.id}}{{tag.name}}{{/linkTo}}</li>
    {{/each}}
  </ul>
</div>

<div class="patterns-content">
  <h1>Patterns</h1>
  {{outlet}}
</div>
